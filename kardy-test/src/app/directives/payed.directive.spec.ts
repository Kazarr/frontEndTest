import { PayedDirective } from './payed.directive';

describe('PayedDirective', () => {
  it('should create an instance', () => {
    const directive = new PayedDirective();
    expect(directive).toBeTruthy();
  });
});
